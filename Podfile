# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
install! 'cocoapods', :preserve_pod_file_structure => true
target 'ChartDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ChartDemo
  pod 'Charts', :git => 'https://github.com/wzio/Charts', :branch => 'roundcorner'
  target 'ChartDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ChartDemoUITests' do
    # Pods for testing
  end

end
